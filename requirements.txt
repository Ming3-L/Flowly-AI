#
# 根目录 requirements.txt（推荐入口）
# -----------------------------
# 这个文件用于“一键安装项目依赖”，避免出现多份依赖清单各自漂移。
#
# - 后端必须依赖：Backend/requirements.txt
# - 桌面屏幕代理（可选）：已并入 Backend/requirements.txt 的可选分组（Desktop Screen Agent）
#
# 用法（Windows / Linux / macOS 一致）：
#   pip install -r requirements.txt
#

-r Backend/requirements.txt

# 可选：如果你需要在 Windows 机器上运行“桌面屏幕代理”（截图 + YOLO + 自动回复发送），
# 直接安装 Backend/requirements.txt 即可；但 torch（尤其 GPU 版）请按 README/脚本从官方源安装。
