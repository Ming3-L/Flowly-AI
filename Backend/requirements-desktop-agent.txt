# Desktop screen agent (YOLO). Do NOT pip install torch from this mirror alone (CPU wheel).
# Use script (recommended): Backend/scripts/install_desktop_agent_gpu_cn.ps1
# Manual order:
#   python -m pip install --no-cache-dir torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   python -m pip install --no-cache-dir -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements-desktop-agent.txt
# Use cu118/cu124 in URL if that matches your driver (see pytorch.org).

requests>=2.28.0
Pillow>=10.0.0
pyautogui>=0.9.54
ultralytics>=8.0.0

# OCR：openocr-python 已写入 Backend/requirements.txt（与后端同环境）；此处不再重复列出。
